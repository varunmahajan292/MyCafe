<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mycafe">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".global_vars"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Customer_Order_Detail" android:parentActivityName=".View_order"></activity>
        <activity android:name=".View_order" android:parentActivityName=".AdminTasksActivity"/>
        <activity android:name=".ViewReceipt" android:parentActivityName=".StartingActivity"/>
        <activity android:name=".ShowCartActivity" />
        <activity android:name=".StartingActivity" />
        <activity android:name=".AdminTasksActivity" />
        <activity
            android:name=".MainActivity"
            android:parentActivityName=".StartingActivity" />
        <activity
            android:name=".splash"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>